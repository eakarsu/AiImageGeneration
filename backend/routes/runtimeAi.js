'use strict';
const express=require('express');const auth=require('../middleware/auth');const pool=require('../db');const router=express.Router();
function config(){const apiKey=String(process.env.OPENROUTER_API_KEY||'').trim();const model=String(process.env.OPENROUTER_MODEL||'').trim();const base=String(process.env.OPENROUTER_BASE_URL||'').replace(/\/+$/,'');if(!apiKey)throw new Error('OPENROUTER_API_KEY is required');if(!model)throw new Error('OPENROUTER_MODEL is required');if(base!=='https://openrouter.ai/api/v1')throw new Error('OPENROUTER_BASE_URL must be https://openrouter.ai/api/v1');return{apiKey,model,url:`${base}/chat/completions`};}
router.get('/history',auth,async(req,res,next)=>{try{const result=await pool.query('SELECT id,feature,input,output,model,created_at FROM runtime_ai_interactions WHERE user_id=$1 ORDER BY created_at DESC LIMIT 50',[req.user.id]);res.json({history:result.rows});}catch(error){next(error);}});
router.post('/prompt-optimize',auth,async(req,res,next)=>{try{const prompt=String(req.body?.prompt||'').trim();if(!prompt)return res.status(400).json({error:'prompt_required'});const{apiKey,model,url}=config();const response=await fetch(url,{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json','X-Title':'AI Image Generation'},body:JSON.stringify({model,messages:[{role:'system',content:'You are an expert image prompt designer. Return a detailed optimized prompt plus concise composition, lighting, and negative-prompt guidance.'},{role:'user',content:prompt}],max_tokens:700})});if(!response.ok)throw new Error(`OpenRouter API error (${response.status})`);const body=await response.json();const content=String(body.choices?.[0]?.message?.content||'').trim();if(!content)throw new Error('OpenRouter returned an empty response');const saved=await pool.query("INSERT INTO runtime_ai_interactions(user_id,feature,input,output,model) VALUES($1,'prompt-optimize',$2::jsonb,$3::jsonb,$4) RETURNING id",[req.user.id,JSON.stringify({prompt}),JSON.stringify({content}),model]);res.json({content,model,interactionId:saved.rows[0].id});}catch(error){next(error);}});
module.exports=router;
